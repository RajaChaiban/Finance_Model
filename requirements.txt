numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.4.0
pyyaml>=5.4.0
jinja2>=3.0.0
yfinance>=0.2.0
QuantLib>=1.42.0

# Agent layer (Phase 1+)
google-genai>=1.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
sse-starlette>=2.0.0
pydantic>=2.0.0
reportlab>=4.0

# Market-intelligence (RAG) layer — see src/agents/market_intelligence.py.
# All three are gated by MARKET_INTEL_ENABLED; the agent pipeline runs
# without them when the flag is off.
chromadb>=0.4
sentence-transformers>=2.2
loguru>=0.7

# Goldman Sachs gs_quant — optional second pricing engine.
# Local features (instrument builders, BlackScholes, payoffs) work without
# any account. Cloud pricing / live curves require Marquee credentials
# (GS_MARQUEE_CLIENT_ID / GS_MARQUEE_CLIENT_SECRET). See
# src/engines/gs_quant_engine.py and route_with_engine(engine="gs").
gs-quant>=1.0
