[
  {
    "flow": "ticker XSS attempt",
    "url": "GET http://localhost:8002/api/market/spot-price?ticker=NOTAREAL%3CSCRIPT%3EALERT(1)%3C/SCRIPT%3E",
    "status": 400,
    "note": "Backend correctly rejected encoded-script ticker."
  },
  {
    "flow": "ticker whitespace",
    "url": "GET http://localhost:8002/api/market/spot-price?ticker=%20%20%20SPY",
    "status": 400,
    "note": "Whitespace not trimmed by the UI before call."
  },
  {
    "flow": "ticker Cyrillic",
    "url": "GET http://localhost:8002/api/market/spot-price?ticker=%D0%90APL",
    "status": 400,
    "note": "Cyrillic A accepted client-side; 400 only on spot, dividend-yield/historical-volatility returned 200 with garbage."
  },
  {
    "flow": "ticker 5000-char",
    "url": "GET http://localhost:8002/api/market/spot-price?ticker=AAAA...(5000 A)",
    "status": 400,
    "note": "Mixed-status across endpoints (spot 400, others 200). UI does not cap input length."
  },
  {
    "flow": "spot=-100 submit",
    "url": "(no POST — blocked by client validateForm)",
    "status": "blocked-client",
    "note": "Client correctly rejected with 'Spot price must be positive'."
  },
  {
    "flow": "strike=0 submit",
    "url": "(no POST — blocked client-side)",
    "status": "blocked-client",
    "note": "Client correctly rejected with 'Strike price must be positive'."
  },
  {
    "flow": "days=99999 submit (huge T)",
    "url": "POST http://localhost:8002/api/price",
    "status": 400,
    "note": "Backend leaks QuantLib internal date error to user: 'Date's serial number (146142) outside allowed range [367-109574]'."
  },
  {
    "flow": "risk_free_rate=1000 (% client → 10.0 server) submit",
    "url": "POST http://localhost:8002/api/price",
    "status": 200,
    "note": "P1: 1000% rate accepted and priced! No upper bound on rate."
  },
  {
    "flow": "dividend_yield=99999 submit",
    "url": "POST http://localhost:8002/api/price",
    "status": 200,
    "note": "P1: Dividend 99999 accepted, no validation client OR server."
  },
  {
    "flow": "dividend_yield=-100 submit",
    "url": "POST http://localhost:8002/api/price",
    "status": 200,
    "note": "P1: Negative dividend yield accepted client-side and 200 OK from backend."
  },
  {
    "flow": "Run Pricing rapid-double-click race",
    "url": "POST http://localhost:8002/api/price (x2-3 in same task)",
    "status": "200/400 each",
    "note": "P1: disabled={isLoading} only takes effect on next React commit; rapid synchronous clicks all dispatch."
  },
  {
    "flow": "Block /api/agent/sessions via fetch shim, click Copilot Run",
    "url": "POST http://localhost:8002/api/agent/sessions (intercepted)",
    "status": 503,
    "note": "GOOD: copilot-error banner correctly shows 'Error: blocked' detail."
  },
  {
    "flow": "Copilot RFQ XSS attempt (<img src=x onerror=alert(1)>)",
    "url": "POST http://localhost:8002/api/agent/sessions",
    "status": "(intake parsing)",
    "note": "GOOD: React HTML-escaped the payload. No XSS. Backend received literal string."
  }
]
